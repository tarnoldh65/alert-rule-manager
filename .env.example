# Copy to .env and adjust before running `docker compose up`.

POSTGRES_DB=alertmanager
POSTGRES_USER=alertmanager
POSTGRES_PASSWORD=change-me

# First administrator account, created by `python -m app.bootstrap` on the backend host.
ALERT_MANAGER_ADMIN_USERNAME=admin
ALERT_MANAGER_ADMIN_PASSWORD=change-me
